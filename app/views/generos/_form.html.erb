<%= form_for(genero, html: { class: "basic-grey" }) do |f| %>
  <% if genero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genero.errors.count, "error") %> prohibited this genero from being saved:</h2>

      <ul>
      <% genero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h1>Gênero</h1>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit "Cadastrar Gênero", class: "button" %>
    <%= link_to 'Cancelar', generos_path, class: "button" %>
  </div>
<% end %>
