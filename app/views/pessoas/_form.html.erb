<%= form_for(pessoa, html: { class: "basic-grey" }) do |f| %>
  <% if pessoa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pessoa.errors.count, "error") %> prohibited this pessoa from being saved:</h2>

      <ul>
      <% pessoa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h1>Pessoa</h1>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :url_foto %>
    <%= f.text_field :url_foto %>
  </div>

  <div class="actions">
    <%= f.submit "Cadastrar Pessoa", class: "button"%>
    <%= link_to 'Cancelar', pessoas_path, class: "button" %>
  </div>
<% end %>
